{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "cargo build",
            "type": "process",
            "command": "cargo",
            "args": ["build"],
            "group": "build"
        },
        {
            "label": "kill openocd",
            "type": "shell",
            "command": "killall -w openocd || exit 0",
            "group": "build",
        }
    ]
}